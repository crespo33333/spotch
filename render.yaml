services:
  - type: web
    name: spotch-backend
    env: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: spotch-db
          property: connectionString
    autoDeploy: true

databases:
  - name: spotch-db
    databaseName: spotch
    user: spotch_user
    plan: free
